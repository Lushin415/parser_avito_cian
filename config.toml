# ===== ВАЖНО! =====
# Этот файл содержит ТОЛЬКО технические настройки парсера
# Параметры задач (urls, tg_token, tg_chat_id) передаются через API от ParserHub

[avito]
# Уведомления - задаются через API
# tg_token = ""  # Передаётся от ParserHub
# tg_chat_id = []  # Передаётся от ParserHub
vk_token = ""
vk_user_id = []

# URLs - задаются через API
# urls = []  # Передаются от ParserHub

# Количество страниц - задаётся через API
count = 3  # Default, переопределяется через API

# Фильтры
keys_word_white_list = []
keys_word_black_list = []
seller_black_list = []
max_price = 0
min_price = 0
geo = ""

# Технические настройки
proxy_string = ""
proxy_change_url = ""
pause_general = 20
pause_between_links = 2
max_age = 0
max_count_of_retry = 5
ignore_reserv = false
ignore_promotion = false
one_time_start = false
one_file_for_link = false
parse_views = false
save_xlsx = true
use_webdriver = true

# Уведомления администратора при сбое прокси
# bot_token = токен PurserHub бота, user_ids = Telegram ID администраторов
admin_bot_token = "8374925023:AAG9QwKAfXnwDZ4ZvtjP6zaoaqkEXeZn6p8"
admin_user_ids = [338908929]


# ===== CIAN =====
[cian]
# URLs - задаются через API
# urls = []  # Передаются от ParserHub

# Тип сделки: "rent_long" (долгосрочная аренда) или "sale" (продажа)
deal_type = "rent_long"

# Город
location = "Москва"

# Количество страниц - задаётся через API
count = 3  # Default, переопределяется через API

# Фильтры
keys_word_white_list = []
keys_word_black_list = []
seller_black_list = []

# Цена (в рублях)
max_price = 0
min_price = 0

# Площадь (в кв.м)
min_area = 0
max_area = 0

# География
geo = "Москва"

# Максимальный возраст объявления в секундах (0 = не проверять)
max_age = 0

# Технические настройки парсинга
pause_general = 60
pause_between_links = 3
max_count_of_retry = 5

# Использовать браузер для обхода блокировок
use_webdriver = true

# Прокси (если нужно, иначе берётся из [avito])
proxy_string = ""
proxy_change_url = ""

# Уведомления - задаются через API
# tg_token = ""  # Передаётся от ParserHub
# tg_chat_id = []  # Передаётся от ParserHub
# vk_token = ""
# vk_user_id = []

# Сохранение результатов
save_xlsx = false
one_time_start = false
